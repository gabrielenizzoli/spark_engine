components:
  sql: { type: sql, sql: select 'value' as column, encodedAs: { schema: value, type: STRING } }

sinks:
  view: { type: view, name: tmpView }

pipelines:
  - { component: sql, sink: view }