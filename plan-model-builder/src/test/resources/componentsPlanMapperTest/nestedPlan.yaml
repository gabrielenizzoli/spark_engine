components:
  source:
    sql: select explode(array(named_struct("num", 1, "str", "a"), named_struct("num", 2, "str", "b"))) as abc
  wrapperComponentName:
    type: wrapper
    component:
      type: wrapper
      component:
        type: ref

sinks:
  view: { type: view, name: tmpView }

pipelines:
  pipe1: { component: reference, sink: view }
  pipe2: { component: reference2, sink: view }